import { Model } from 'vue-mc';

import { Game } from './game';

export class User extends Model {

    default() {
        return {

        }
    }
    modifiers() {
        return {

        }
    }
    validation() {
        return {

        }
    }
    // _id: string;
    // username: string;
    // totalRuns: number;
    // creationDate: Date;
    // discord?: string;
    // twitch?: string;
    // youtube?: string;
    // steam?: string;
}
